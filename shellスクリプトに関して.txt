サポートしているシェルを確認する方法
$ cat /etc/shells

#  シェルスクリプトを書くときは必ず最初に使用するシェルを指定する
例：
#! /bin/bash

# 注意！実行たいときは必ず権限を変更するのを忘れない！
$ chmod 755 shファイル名

実行するときは以下
./shファイル名

インストールする必要がある！
$ yum install bc
bcコマンドで計算することによって演算が可能になる。
echo "20.5 + 5" | bc
